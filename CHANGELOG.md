# Changelog

## 1.7.0

- `합의 추천 · 선택 방식 중앙값` 추가.
- 선택한 다른 로컬 방식 2개 이상의 번호별 0~1 적합도를 중앙값으로 강건 집계.
- AI는 집계 제외, 명리는 선택된 경우만 집계.
- 중앙값 참여 방식·번호별 중앙값·방식 간 점수 범위·최종 지지표 수를 센서 속성으로 공개.
- 중앙합의는 항상 기초 방식들을 먼저 생성한 뒤 마지막 게임으로 계산.
- 과거 1등 완전일치·추천 중복·재생성 제외 규칙 유지.

## 1.6.0

- 제공 사주 프롬프트의 계산/표와 로또 응용 가중치를 구분하는 명리 규칙 계층 추가.
- 틀린 일진 기준일, 지지십신/지장간표 불일치, 한국/중국 음력 및 절기 시간대 교정·구분.
- 십신10종/지장간 비율/통근/투간/격국 후보/4계열 보완오행/5단계 운/합충형파해 점수 추적.
- 시간 미상 3주 분석, 불확실 대운 점수 제외, 대운 경계를 날짜로 비교.
- 동점 순위·베이지안 수축·이월수·번호대 일관성·가중치 정규화 수정.
- 상위1200 후보 힙, 중복 후보 복귀 차단, 직전 재생성 제외 목록 저장, AI 입력 검증 강화.
- 명리 정보를 AI 프롬프트에서 제외, Recorder의 큰 명리속성 중복 저장 제외.
- 단위/기존 회귀시험, 실제 HA OptionsFlow 스모크시험, 26회차×16방식 탐색적 과거 재생 추가.
- 테스트·HA 호환·Hassfest 모두 성공한 동일 커밋만 자동 HACS Release 생성.


## 1.5.2 — 2026-09-11

### Fixed
- Home Assistant 모바일/웹에서 명리 방식을 선택한 뒤 다음 단계로 전환할 때 반복되던 `Unknown error occurred` 경로 제거
- 개인 사주정보 입력을 별도 2단계 Options Flow가 아니라 **동일 구성 화면에 직접 표시**하도록 변경
- 명리 방식을 선택한 경우에만 생년월일·출생시간·양/음력·성별·출생지·시간대 필수 검증 수행
- 옵션 처리 중 예상하지 못한 예외도 구성 화면의 사용자 오류로 반환해 일반 `Unknown error` 화면을 피하도록 보강
- `추천 방식 안내`가 로또 데이터 갱신 실패와 함께 `사용할 수 없음`이 되던 문제 수정

### Changed
- `추천 방식 안내` 상태값 `16`을 `16개 추천 방식`으로 변경해 의미를 명확히 표시
- 선택된 추천 센서마다 `method_category`, `method_description` 속성을 추가
- 명리 사주 프로필 센서에 실제 입력 위치 안내를 추가하고 데이터 소스 장애와 무관하게 설정 상태를 확인할 수 있도록 개선
- 구성 화면의 명리 입력 필드에 `[명리]` 접두어와 상세 설명을 추가

## 1.5.1 — 2026-09-11

### Fixed
- 여러 추천 방식을 추가/제거한 뒤 확인할 때 Home Assistant 모바일에서 `Unknown error occurred`가 표시될 수 있던 Options Flow 처리 보강
- 명리 단계의 선택적 경도 값이 비어 있을 때 `suggested_value: null` 형태로 프론트엔드에 전달되지 않도록 수정
- 복수 선택값을 list/tuple 모두 안전하게 정규화하고 잘못된 선택값은 사용자 오류로 표시하도록 개선
- `명리 권장 · 개인 사주 원국·대운·추첨일` 선택 후 확인하면 `개인 사주정보 입력` 단계로 확실하게 전환하도록 흐름을 명시화
- Home Assistant 브랜드 로고/아이콘 크기 규격을 맞춰 모바일에서 찌그러지거나 깨져 보이던 문제 수정

### Changed
- 가로형 원본 PNG는 `images/logo-horizontal.png`를 canonical source로 유지하며 새 그림을 만들지 않음
- 정사각형 아이콘은 가로 PNG의 왼쪽 복주머니·6·45 공 영역만 실제 투명 픽셀 경계로 크롭해 256/512px로 생성
- 이전에 별도로 다시 그렸던 SVG 로고/아이콘 자산 제거
- Home Assistant용 가로 브랜드 이미지는 동일 PNG 픽셀을 비율 유지 리사이즈해 normal/@2x 규격으로 생성

## 1.5.0 — 2026-09-11

### Added
- 명리 권장 사용 전 필수 개인 사주정보 입력 단계
- lunar_python 1.4.8 기반 연·월·일·시주, 절기 기준 팔자, 지장간, 십신, 납음, 12운성 계산
- 성별에 따른 대운 순·역행 및 현재 대운 계산
- 추첨일 20:35 사주와 원국의 천간합·충, 지지합·충·해·형 관계 분석
- 음력/윤달 입력, IANA 시간대, 선택적 진태양시 경도·균시차 보정
- 명리 사주 프로필 상태/상세 센서

### Changed
- 명리 권장을 `개인 사주 원국·대운·추첨일` 방식으로 고도화
- 개인 사주정보가 없으면 명리 추천 센서를 생성하지 않고 `설정 필요` 상태만 표시
- 신규 기본 추천 목록에서 명리 방식을 제외하여 명시적 선택+프로필 입력 후에만 활성화
- 명리 점수를 주 신호로 높이고 로또 통계 점수는 보조 신호로 축소
- HA AI 추천 프롬프트에 원본 생년월일·출생시간·출생지를 전달하지 않도록 명시

## 1.4.0 — 2026-09-11

### Added

- Home Assistant Retina/고밀도 표시용 `brand/logo@2x.png`, `brand/icon@2x.png`
- `독창 패턴 · 다중시간대 공명`
- `공개 공식 · 삼중 동반출현`
- `공개 공식 · 지수감쇠 최근성`
- `공개 공식 · 베이지안 수축`
- `공개 공식 · 재등장 주기`
- 전체 16개 추천 방식의 이름·분류·상세 원리를 제공하는 `추천 방식 안내` 센서
- 수동 번호 재생성 순번을 저장해 동일 회차에서도 고득점 후보군 내 다른 조합을 선택하는 기능

### Changed

- `독창 패턴 ①`, `독창 패턴 ②`를 각각 `독창 패턴 · 위상잔차 그래프`, `독창 패턴 · 전이·간격 위상`으로 변경
- `공식 탐색 ③ · 공개공식 종합형`을 `공개 공식 · 종합 앙상블`로 변경
- 추천 방식 선택 항목과 문서 설명을 더 상세하게 확장
- `즉시 새로고침`을 `즉시 새로고침 · 번호 재생성`으로 변경
- 수동 새로고침 시 선택된 비AI 추천을 재생성하되 기존 AI 추천은 유지하고 AI 자동 호출도 하지 않도록 변경
- 종합 앙상블에 최근성 감쇠·베이지안 수축·삼중 동반출현 신호를 추가
- 분석 엔진 버전을 `selectable_multi_formula_v4`로 갱신

### Fixed

- Home Assistant 모바일/Retina 화면에서 가로 로고와 정사각 아이콘이 깨져 보이던 브랜드 자산 문제 수정
- 데이터가 바뀌지 않은 상태에서 `즉시 새로고침`을 눌러도 추천번호가 동일하게 유지되던 문제 수정
- v1.4.0 정식 배포 전에 Python 컴파일·분석 회귀 테스트·Hassfest를 모두 통과하도록 검증

## 1.3.0 — 2026-09-11

### Added

- GitHub 공유 이력 미러 `data/lotto645-history.json`
- HACS 릴리스에 포함되는 `history_seed.json` 오프라인/장애 폴백
- SHA-256, 1회~최신 연속성, 번호 범위·중복·보너스 검증
- 토요일 22:50 KST + 일요일 09:30 KST 중앙 미러 갱신 워크플로
- 선택적 `동행복권 제한적 직접 폴백` 옵션(기본 OFF)
- HACS 과거 축약 커밋 참조 `ad8f096` 호환 경로

### Changed

- HA 런타임의 동행복권 전체 회차 직접 크롤링 제거
- 기본 데이터 경로를 `HA .storage → GitHub 공유 미러 → 번들 스냅샷` 중심으로 변경
- 중앙 미러 갱신은 공개 GitHub 역사 데이터셋을 벌크 운반용으로 사용하고 최신 회차만 동행복권과 교차 검증
- 동행복권 직접 접근은 순차 요청, 최소 3초 간격, 실행당 최대 4회, 최근 최대 2개 누락 회차로 제한
- HTTP 403/429 발생 시 직접 요청 회로 차단기를 활성화하며 우회·프록시 회전·CAPTCHA 회피를 사용하지 않음
- 추천 방식 복수 선택, 공개공식 종합형, 명리 권장, Home Assistant AI Task, 로고 및 기존 캐시 호환 기능을 모두 유지

### Fixed

- 최초 설정 시 동행복권 전체 이력 요청으로 `동행복권 요청 시간이 초과되었습니다`가 발생할 수 있던 구조 제거
- HACS가 과거 `ad8f096` 축약 커밋을 브랜치처럼 내려받으려다 404가 발생하는 기존 설치 호환 문제 보완

## 1.2.1 — 2026-09-11

### Fixed

- 제공된 Lotto 6/45 가로 로고를 고해상도 투명 PNG로 다시 적용
- README 및 통합 브랜드 로고를 동일 자산으로 정리

## 1.2.0 — 2026-09-11

### Added

- `명리 권장 · 일진 오행·하도 수리` 추천 방식
- 다음 추첨 예정일의 60갑자 일주 계산
- 일간(Day Master)·지지 오행과 하도(河圖) 수리오행 결합
- 오행 분산·음양 균형·오행 과집중 억제 점수
- 1949-10-01 갑자(甲子) 기준점과 하도 오행 대응 회귀 테스트

### Changed

- 기본 추천 방식에 명리 권장안을 포함
- 분석 엔진 버전을 `selectable_multi_formula_v3`로 갱신
- 공개 분석식과 전통 명리 방식을 별도 카테고리로 분리
- 전통 명리 방식의 “검증”은 계산 규칙 재현성을 뜻하며 당첨 확률 상승에 대한 과학적 검증이 아님을 문서화

## 1.1.0 — 2026-09-11

### Added

- Home Assistant 옵션 화면에서 추천 방식 복수 선택
- 독창 분석 2종과 공개 분석식 8종
- `공식 탐색 ③ · 공개공식 종합형`
- Home Assistant `ai_task` 기반 AI 번호 추천
- 기본 데이터 생성 AI Task 또는 지정 AI Task 선택
- AI 결과의 번호 범위·중복·과거 1등 완전일치 검증
- AI 추천 수동 버튼, 서비스 및 새 회차 자동 생성 옵션
- 가로형 Lotto 6/45 로고와 복주머니 정사각형 아이콘
- 선택 방식별 동적 센서 및 v1.0 엔티티 마이그레이션
- 분석 엔진 회귀 테스트

### Changed

- 조합 탐색을 상위 후보 제한 방식으로 변경하여 메모리 사용량 절감
- 기존 1회~최신 회차 캐시를 그대로 유지하도록 저장 형식 호환
- 공개 공식은 통계 필터일 뿐 당첨 확률을 높이는 공식이 아님을 UI와 문서에 명시
